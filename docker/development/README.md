# Development Docker

Dipakai oleh `docker-compose.yml` di root monorepo (`include`). Jalankan dari **akar repo** `metroapparel`:

- `pnpm docker:up` / `pnpm docker:down` / `pnpm docker:logs`

Path `build`, `volumes`, dan `env_file` relatif terhadap folder file ini (`docker/development/`).
